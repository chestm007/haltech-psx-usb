See `PROTOCOL.md` for the active Haltech protocol notebook.

Current status:
- Static RE has confirmed the ECU Manager is a .NET serial client and has mapped several request command bytes.
- Live baud/framing confirmation is still pending.
- Earlier probe-timeout notes are stale; keep protocol facts in `PROTOCOL.md` instead.
